export const environment = {
  production: false,
  /**
   * Same-origin prefix; Angular dev server proxies to spring-backend (see proxy.conf.json).
   * Requests: /api/ask → http://127.0.0.1:8080/ask
   */
  apiUrl: "/api"
};
