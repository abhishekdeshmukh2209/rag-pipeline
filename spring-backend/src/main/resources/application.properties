spring.application.name=rag-pipeline-spring
server.port=8080

# Angular dev server (frontend/) proxies /api/* here when using environment.development.ts + proxy.conf.json
app.cors.allowed-origins=http://localhost:4200,http://127.0.0.1:4200
